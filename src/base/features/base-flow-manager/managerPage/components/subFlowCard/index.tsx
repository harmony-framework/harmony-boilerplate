import * as React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface Props {

}

const subFlowCard: React.FC<Props> = (props: Props) => {
	return (
		<div>
			write here your own core component
		</div>
	);
};

export default subFlowCard;
