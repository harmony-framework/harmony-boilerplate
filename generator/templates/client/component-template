import * as React from 'react';
import { withLocalize, LocalizeContextProps } from 'react-localize-redux';

export type Props = {

};

const <%= name %>: React.FC<Props & LocalizeContextProps> = (props: Props& LocalizeContextProps) => {
	return (
		<div>
			write here your own core component
		</div>
	);
};

export default withLocalize<Props & LocalizeContextProps>(<%= name %>);
